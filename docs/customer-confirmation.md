# 客户确认清单

## 数据访问

1. 请提供 SQL Server 专用只读账号，避免使用 `sa`。
2. 请确认该账号可访问数据库 `UFTData662308_000088` 的系统元数据与业务表。
3. 请确认允许读取全部表结构、字段、索引和估算行数。
4. 请确认允许对候选核心表做有限抽样，建议每表 1000 行起步。
5. 请确认允许本地生成离线 HTML/CSV 报告。

## 业务范围

1. T+ 当前启用了哪些模块：库存、采购、销售、应收应付、项目、生产等。
2. 老板最关心的 3 到 5 个经营问题是什么。
3. 当前人工统计报表有哪些，最好提供 Excel 样例。
4. 哪些数据目前被认为“不准”，是否有典型单据或案例。
5. 现场、仓库、文员录入分别负责哪些数据。

## 数据安全

1. 哪些字段属于敏感字段，需要脱敏或不展示。
2. 是否允许展示样例记录；如果允许，样例记录最多展示多少行。
3. 报告是否可带出客户现场。
4. 后续是否需要用户权限隔离。

## 口径确认

1. 空值是否一定代表问题，还是部分字段业务允许为空。
2. 负数库存、负数金额是否允许存在。
3. 异常日期的判断边界，例如早于 2000 年或晚于当前日期是否视为异常。
4. 单据状态、审核状态、关闭状态分别如何理解。
5. 数据及时性的要求，例如当天业务是否必须当天录入。

> 数据字典仅作为辅助解释材料，不作为最终判断依据。实际分析以客户真实数据库扫描结果和客户确认的业务口径为准。
